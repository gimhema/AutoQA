[package]
name = "ouroboros-link"
version = "0.1.0"
edition = "2024"
description = "Game-side SDK for communicating with the Ouroboros QA agent"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
